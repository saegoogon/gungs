{
  "name": "wifi-spot",
  "version": "1.0.0",
  "private": true,
  "description": "Interactive Wi-Fi landing page with signal guidance and placement tips.",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check public/app.js"
  }
}
